import { ComingSoon } from "@/components/shared/coming-soon";

export default function AIAdvisorPage() {
  return (
    <ComingSoon
      title="AI Career Advisor"
      description="A conversational assistant grounded in your own data — never inventing achievements."
      phaseNote="Shipping in Phase 5, once there is enough structured data (career, projects, MBA readiness) for it to reason over."
    />
  );
}
