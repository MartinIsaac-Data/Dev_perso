<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Les autorisations, et pourquoi chacune est là. Une autorisation demandée
      sans raison lisible est un refus de plus au premier lancement.
    -->

    <!-- Le geste du produit. Sans elle, il ne reste qu'un carnet de notes. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- La capture part sur le serveur pour être transcrite. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      Rappels (`flutter_local_notifications`).

      POST_NOTIFICATIONS est exigée à partir d'Android 13 et demandée à
      l'exécution ; RECEIVE_BOOT_COMPLETED permet de reprogrammer les rappels
      après un redémarrage, sans quoi tout ce qui était prévu disparaît avec
      l'extinction.

      SCHEDULE_EXACT_ALARM plutôt que USE_EXACT_ALARM : la première se demande à
      l'utilisateur, la seconde s'obtient sans le prévenir mais expose
      l'application à un audit de la boutique. Un rappel qui glisse de quelques
      minutes est un désagrément ; un retrait de la boutique n'en est pas un.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="MindFlow"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
          Les deux récepteurs de `flutter_local_notifications`. Le second est
          celui qui reprogramme les rappels après un redémarrage : sans lui,
          éteindre son téléphone efface silencieusement tout ce qui était prévu.
        -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
