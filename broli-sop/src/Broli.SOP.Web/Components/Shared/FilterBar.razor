@implements IDisposable
@inject FilterState Filters

@if (Filters.Options is { } o)
{
    var f = Filters.Filter;
    <div class="filterbar" aria-label="Global filters">
        <span class="lbl">Filters</span>
        <div class="fsel">
            <select class="fsel-select" style="height:32px;border:1px solid var(--border);border-radius:7px;background:var(--surface);padding:0 6px"
                    aria-label="Year" value="@(f.Year?.ToString() ?? "")" @onchange="SetYear">
                <option value="">Year: latest</option>
                @foreach (var y in o.Years.OrderDescending()) { <option value="@y">@y</option> }
            </select>
        </div>
        <div class="fsel">
            <button type="button" class="@(f.Months.Count > 0 ? "on" : "")" @onclick="() => _months = !_months">
                Month@(f.Months.Count == 1 ? $": {MonthNames[f.Months[0] - 1]}" : f.Months.Count > 1 ? "" : ": latest")
                @if (f.Months.Count > 1) { <span class="count">@f.Months.Count</span> }
                <span class="caret">▼</span>
            </button>
            @if (_months)
            {
                <div class="backdrop" @onclick="() => _months = false"></div>
                <div class="fpanel" style="min-width:240px">
                    <div class="months">
                        @for (var m = 1; m <= 12; m++)
                        {
                            var month = m;
                            <button type="button" class="@(f.Months.Contains(month) ? "on" : "")" @onclick="() => ToggleMonth(month)">@MonthNames[month - 1]</button>
                        }
                    </div>
                    <div class="actions">
                        <button type="button" @onclick="ClearMonths">Latest month</button>
                        <button type="button" @onclick="() => _months = false">Done</button>
                    </div>
                </div>
            }
        </div>
        <MultiSelect Label="Agency" Options="o.Agencies" Selected="f.Agencies" SelectedChanged="v => Set(x => x.Agencies = v)" />
        <MultiSelect Label="Product Family" Options="o.Categories" Selected="f.Categories" SelectedChanged="v => Set(x => x.Categories = v)" />
        <MultiSelect Label="Brand" Options="o.Brands" Selected="f.Brands" SelectedChanged="v => Set(x => x.Brands = v)" />
        <MultiSelect Label="Material Type" Options="o.MaterialTypes" Selected="f.MaterialTypes" SelectedChanged="v => Set(x => x.MaterialTypes = v)" />
        @if (_more)
        {
            <MultiSelect Label="Supplier" Options="o.Suppliers" Selected="f.Suppliers" SelectedChanged="v => Set(x => x.Suppliers = v)" />
            <MultiSelect Label="Country" Options="o.Countries" Selected="f.Countries" SelectedChanged="v => Set(x => x.Countries = v)" />
            <MultiSelect Label="Status" Options="o.Statuses" Selected="f.Statuses" SelectedChanged="v => Set(x => x.Statuses = v)" />
            <ProductPicker />
        }
        <button class="link-btn" style="font-size:12.5px" @onclick="() => _more = !_more">@(_more ? "− Fewer" : "+ More filters")</button>

        @if (f.ActiveCount > 0)
        {
            <div class="chips" aria-label="Active filters">
                @if (f.Year is { } y) { <Chip Name="Year" Value="@y.ToString()" OnRemove="@(() => Set(x => x.Year = null))" /> }
                @foreach (var m in f.Months) { var mm = m; <Chip Name="Month" Value="@MonthNames[mm - 1]" OnRemove="@(() => Set(x => x.Months.Remove(mm)))" /> }
                @Chips("Agency", f.Agencies, (x, v) => x.Agencies.Remove(v))
                @Chips("Family", f.Categories, (x, v) => x.Categories.Remove(v))
                @Chips("Brand", f.Brands, (x, v) => x.Brands.Remove(v))
                @Chips("Product", f.Products, (x, v) => x.Products.Remove(v))
                @Chips("Supplier", f.Suppliers, (x, v) => x.Suppliers.Remove(v))
                @Chips("Country", f.Countries, (x, v) => x.Countries.Remove(v))
                @Chips("Material", f.MaterialTypes, (x, v) => x.MaterialTypes.Remove(v))
                @Chips("Status", f.Statuses, (x, v) => x.Statuses.Remove(v))
                <button class="reset" @onclick="Filters.ResetAsync">✕ Reset filters</button>
            </div>
        }
    </div>
}

@code {
    public static readonly string[] MonthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
    private bool _months, _more;

    protected override void OnInitialized()
    {
        Filters.Changed += OnChanged;
        Filters.OptionsChanged += OnOptions;
    }

    private void OnOptions() => InvokeAsync(StateHasChanged);

    private Task OnChanged() => InvokeAsync(StateHasChanged);

    private Task Set(Action<SopFilter> mutate) => Filters.UpdateAsync(mutate);

    private Task SetYear(ChangeEventArgs e) => Set(x => x.Year = int.TryParse(e.Value?.ToString(), out var y) ? y : null);

    private Task ToggleMonth(int m) => Set(x => { if (!x.Months.Remove(m)) x.Months.Add(m); x.Months.Sort(); });

    private Task ClearMonths() { _months = false; return Set(x => x.Months.Clear()); }


    private RenderFragment Chips(string name, List<string> values, Action<SopFilter, string> remove) => __builder =>
    {
        foreach (var v in values)
        {
            var value = v;
            <Chip Name="@name" Value="@Filters.LabelOf(name, value)" OnRemove="@(() => Set(x => remove(x, value)))" />
        }
    };

    public void Dispose()
    {
        Filters.Changed -= OnChanged;
        Filters.OptionsChanged -= OnOptions;
    }
}
